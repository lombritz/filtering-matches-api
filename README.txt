

Download mongo docker image `docker pull mongo`
Build command `./mvnw package -Dspring.profiles.active=local`
Run with docker-compose `docker-compose up -d`

Default MongoDB user credentials
User: testuser
Password: p@55W0Rd

Run tests
Run integration tests
